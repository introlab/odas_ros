<launch>
  <!-- Arguments -->
  <arg name="configuration_path" default="$(find odas_ros)/config/configuration.cfg"/>
  <arg name="ssl_enabled" default="true"/>
  <arg name="sst_enabled" default="true"/>
  <arg name="sss_enabled" default="true"/>
  <arg name="visualization" default="true"/>
  <arg name="rviz_cfg" default="-d $(find odas_ros)/config/rviz/odas_rviz.rviz"/>

  <!-- ODAS -->
  <group ns="odas">
    <node pkg="odas_ros" type="odas_core_node" name="odas_core_node">
          <param name="configuration_path" value="$(arg configuration_path)"/>
    </node>

    <node pkg="odas_ros" type="odas_server_node.py" name="odas_server_node" output="screen">
      <param name="configuration_path" value="$(arg configuration_path)"/>
      <param name="ssl_frame_id" value="odas"/>
      <param name="sst_frame_id" value="odas"/>

      <param name="ssl_enabled" value="$(arg ssl_enabled)"/>
      <param name="sst_enabled" value="$(arg sst_enabled)"/>
      <param name="sss_enabled" value="$(arg ssl_enabled)"/>
    </node>
    <group if="$(arg visualization)">
      <node pkg="odas_ros" type="odas_visualization_node.py" name="odas_visualization_node" output="screen">
        <param name="ssl_enabled" value="$(arg ssl_enabled)"/>
        <param name="sst_enabled" value="$(arg sst_enabled)"/>
      </node>

      <!-- Position of frame ODAS relative to robot Base_link -->
      <node if="$(arg visualization)" pkg="tf" type="static_transform_publisher" name="odas_broadcaster" args="0 0 0 0 0 0 base_link odas 100" />

      <!--Run Rviz-->
      <node name="rviz" pkg="rviz" type="rviz" args="$(arg rviz_cfg)"/>
    </group>
  </group>
</launch>
